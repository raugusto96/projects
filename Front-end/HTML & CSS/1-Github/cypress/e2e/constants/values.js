// Paths
const PATH = './src/index.html';

// Seletores
const formElement = '#login-form';
const emailInputElement = '#email-input';
const passwordInputElement = '#password-input';
const loginButtonElement = '#login-button';

// Expects
const emailExample = 'email@example.com';
const passwordExample = 'example123';

export default {
  PATH,
  formElement,
  emailInputElement,
  passwordInputElement,
  loginButtonElement,
  emailExample,
  passwordExample,
};